// Copyright 2026, Pulumi Corporation.  All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

package apitype

import "fmt"

// Whether the caller's permissions limited what a graph traversal could see. It reports permissions only; 'complete' does not certify
// result fidelity (see resultMode) or index freshness.
type GraphVisibility string

const (
	// Every node and edge the traversal touched was within the caller's permissions.
	GraphVisibilityComplete GraphVisibility = "complete"

	// At least one traversal step encountered infrastructure outside the caller's permissions, or the engine could not verify completeness for
	// this run, so the answer may be missing dependencies, dependents, or stacks. No detail about what is hidden is included. Treat a trimmed
	// result as disqualifying for any question that depends on completeness, such as impact analysis or an absence check; a caller with wider
	// permissions can run the same query for a complete answer.
	GraphVisibilityTrimmed GraphVisibility = "trimmed"
)

func (v GraphVisibility) AllValues() []GraphVisibility {
	return []GraphVisibility{
		GraphVisibilityComplete,
		GraphVisibilityTrimmed,
	}
}

func (v GraphVisibility) IsValid() bool {
	switch v {
	case GraphVisibilityComplete:
		return true
	case GraphVisibilityTrimmed:
		return true
	}

	return false
}

func (v GraphVisibility) openapiName() string {
	switch v {
	case GraphVisibilityComplete:
		return "Complete"
	case GraphVisibilityTrimmed:
		return "Trimmed"
	}

	return ""
}
func (v GraphVisibility) GoString() string {
	if s := v.openapiName(); s != "" {
		return "apitype.GraphVisibility" + s
	}
	return fmt.Sprintf("apitype.GraphVisibility(%#v)", string(v))
}
